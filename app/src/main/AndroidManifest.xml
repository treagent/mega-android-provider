<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MegaProviderApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_mega"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MegaProvider">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MegaProvider">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".MegaDocumentsProvider"
            android:authorities="com.mega.provider.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.provider.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

            <activity android:name=".PhotoViewerActivity" android:label="Photos" android:parentActivityName=".FileBrowserActivity" />
        <activity android:name=".FileBrowserActivity" android:label="MEGA Files" android:parentActivityName=".LoginActivity" />

        <service
            android:name=".MegaLoadService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    </application>

</manifest>
